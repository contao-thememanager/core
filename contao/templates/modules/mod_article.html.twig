{% extends '@Contao/mod_article' %}

{% set class = (class ~ ' grid')|trim %}

{% block content %}
    {% if not noMarkup %}
        <div class="inside article_inside">
            {{ parent() }}
        </div>
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}
